# Copy this file to a new file named ".env" and fill in your credentials for local development.
# The .env file is ignored by Git and will NOT be committed.

# --- Kite Connect Credentials ---
# Get these from your Kite Developer Console and by generating a token.
KITE_API_KEY="your_api_key_here"
KITE_ACCESS_TOKEN="your_generated_access_token_here"

# --- Google Sheets Credentials ---
# Paste the entire JSON content of your service account key here, enclosed in single quotes.
GOOGLE_SHEETS_CREDENTIALS='{"type": "service_account", "project_id": "...", ...}'